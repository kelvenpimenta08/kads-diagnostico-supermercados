/* ============================================================
   K ADS - CONFIGURACAO
   Este e o unico arquivo que voce precisa editar para publicar.
   ============================================================ */

window.KADS_CONFIG = {

  /* 1. URL do Google Apps Script publicado como "app da web".
     Passo a passo completo no README.md
     Enquanto estiver vazio, o formulario roda em MODO TESTE:
     ele valida tudo e mostra o resultado no console, sem gravar na planilha. */
  endpoint: "https://script.google.com/macros/s/AKfycbx93g2pGRdagT1G_fNqAaxi8x7hBNgDmM4W85gyH_tZt_09IZP1ky-UZLvPb1xqtCOs/exec",

  /* 2. WhatsApp que aparece na tela final, formato internacional, so numeros. */
  whatsapp: "5524993206348",

  /* 3. Mensagem pre-preenchida do botao de WhatsApp da tela final. */
  whatsappMensagem: "Olá! Acabei de preencher o diagnóstico da K Ads.",

  /* 4. Nome da marca exibido no topo. */
  marca: "K Ads",

  /* 5. Salvar rascunho no navegador para o lead poder voltar depois. */
  salvarRascunho: true
};
